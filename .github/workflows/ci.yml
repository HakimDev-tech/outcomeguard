name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  scaffold:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "OutcomeGuard CI scaffold"
